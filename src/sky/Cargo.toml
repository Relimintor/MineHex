[package]
name = "bevy_sky_gradient"
version = "0.1.0"
edition = "2021"
description = "WASM sky gradient helpers for MineHex"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
